<Application x:Class="NvidiaCi.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NvidiaCi">
    <Application.Resources>
        <!-- NVIDIA Industrial Color Palette -->
        <SolidColorBrush x:Key="AccentColor" Color="#E60012" /> <!-- NVIDIA Red (RTX) -->
        <SolidColorBrush x:Key="AccentHover" Color="#FF1A1A" />
        <SolidColorBrush x:Key="BackgroundColor" Color="#E6000000" /> <!-- Almost black with slight transparency -->
        <SolidColorBrush x:Key="CardBackground" Color="#1A1A1A" />
        <SolidColorBrush x:Key="SecondaryBackgroundColor" Color="#0D0D0D" />
        <SolidColorBrush x:Key="TextColor" Color="#FFFFFF" />
        <SolidColorBrush x:Key="TextSecondary" Color="#B0B0B0" />
        <SolidColorBrush x:Key="BorderColor" Color="#2A2A2A" />

        <!-- Game Item Template (Modern Card with hover glow) -->
        <DataTemplate x:Key="GameItemTemplate">
            <Grid Width="140" Margin="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="180" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" Background="{StaticResource CardBackground}" CornerRadius="12" Margin="0,0,0,12" 
                        x:Name="ImageBorder" BorderThickness="1" BorderBrush="{StaticResource BorderColor}">
                    <Grid>
                        <!-- Red glow on hover -->
                        <Border x:Name="GlowEffect" CornerRadius="12" BorderBrush="{StaticResource AccentColor}" 
                                BorderThickness="2" Opacity="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#E60012" BlurRadius="20" ShadowDepth="0" Opacity="0.8"/>
                            </Border.Effect>
                        </Border>
                        
                        <TextBlock Text="🎮" VerticalAlignment="Center" HorizontalAlignment="Center" 
                                   FontSize="56" Opacity="0.15"/>
                        
                        <!-- Play overlay on hover -->
                        <Border x:Name="PlayOverlay" Background="#CC000000" CornerRadius="12" Opacity="0">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="▶" Foreground="{StaticResource AccentColor}" FontSize="36" 
                                           HorizontalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Text="LAUNCH" Foreground="White" FontSize="10" 
                                           HorizontalAlignment="Center" Margin="0,4,0,0" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
                
                <TextBlock Grid.Row="1" Text="{Binding Name}" Foreground="{StaticResource TextColor}" 
                           FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center" TextAlignment="Center" 
                           TextWrapping="Wrap" MaxHeight="40" LineHeight="16" />
            </Grid>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                    <Setter TargetName="GlowEffect" Property="Opacity" Value="1" />
                    <Setter TargetName="PlayOverlay" Property="Opacity" Value="1" />
                    <Setter TargetName="ImageBorder" Property="Background" Value="#252525" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <!-- Premium Slider Style (NVIDIA Red) -->
        <Style x:Key="NvidiaSliderStyle" TargetType="Slider">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Border x:Name="TrackBackground" Grid.Row="1" Height="4" Background="{StaticResource BorderColor}" 
                                    CornerRadius="2" VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track" Grid.Row="1">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4" Background="{StaticResource AccentColor}" CornerRadius="2"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="18" Height="18">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="White" Stroke="{StaticResource AccentColor}" StrokeThickness="3">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect Color="#E60012" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBoxItem Style -->
        <Style x:Key="GameItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Filter Preset Button Style (Industrial) -->
        <Style x:Key="FilterPresetButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#252525"/>
                                <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sidebar Navigation Button Style (Industrial) -->
        <Style x:Key="NavButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Width" Value="80" />
            <Setter Property="Height" Value="80" />
            <Setter Property="FontSize" Value="28" />
            <Setter Property="Foreground" Value="#505050" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" x:Name="Border">
                            <Grid>
                                <Rectangle x:Name="Indicator" Fill="{StaticResource AccentColor}" 
                                           HorizontalAlignment="Left" Width="3" Visibility="Collapsed"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource AccentColor}" />
                                <Setter TargetName="Indicator" Property="Visibility" Value="Visible" />
                                <Setter TargetName="Border" Property="Background" Value="#1A1A1A" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#151515" />
                                <Setter Property="Foreground" Value="#AAAAAA" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
